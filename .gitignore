.git
.idea
node_modules
nbproject
admin/i18n/flat.txt
admin/i18n/*/flat.txt
# Runtime artefacts the adapter writes next to the compiled backend
/build/lib/*.bat
/build/lib/*.log
/build/lib/*.sh
/build/lib/restore.json

# Source maps of the compiled backend. The generated .js is committed (see tsconfig.build.json),
# the maps are pure build artefacts and are regenerated by `npm run tsc`.
/build/**/*.js.map

.commitinfo
.mount
.DS_Store


# ioBroker dev-server
.dev-server/
/src-admin/.__mf__temp
